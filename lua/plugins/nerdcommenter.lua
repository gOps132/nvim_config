-- Inside lua/plugins/plugins/config.lua

return {
  {
    'preservim/nerdcommenter',
    config = function()
      -- Optional configuration for NERDCommenter, if needed
      -- For example, you can set custom key mappings here
    end,
  },
  -- Add other plugins here
}

